[package]
name = "arm-lab-demo"
description = "Demos: UR5e IK servo (demo1), RRT-Connect pillar dodge (demo2), pick-and-place (demo3)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
arm-lab = { path = "../arm-lab" }
nalgebra = { workspace = true }
mujoco-rs = { workspace = true, features = ["renderer"] }
rerun = "0.36"

[lints]
workspace = true
